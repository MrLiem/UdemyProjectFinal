class Order {
    constructor(id, courses, totalAmount, orderDate) {
        this.id = id;
        this.courses = courses;
        this.totalAmount = totalAmount;
        this.orderDate = orderDate;
    }

}

export default Order